# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt
# HR Attrition DSS — Groq + HuggingFace Migration Edition
# ─────────────────────────────────────────────────────────────────────────────
# Install:  pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── A) Core ML / DSS Model Stack ─────────────────────────────────────────────
streamlit>=1.35.0,<2.0.0
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0
scikit-learn>=1.3.0,<2.0.0
xgboost>=2.0.0,<3.0.0
imbalanced-learn>=0.11.0,<1.0.0

# ── B) Groq + LangChain Ecosystem (golden-lock) ──────────────────────────────
# Pinned exactly to preserve RetrievalQA chain structure and
# langchain_core.pydantic_v1 compatibility across all sub-packages.
langchain==0.2.17
langchain-core==0.2.43
langchain-community==0.2.19
langchain-groq==0.1.9

# ── C) Local Embeddings & Vector Store Stack ──────────────────────────────────
# OpenAI / tiktoken removed. Embeddings run locally via HuggingFace.
langchain-chroma==0.1.4
chromadb==0.5.3
sentence-transformers>=2.7.0,<3.0.0
pypdf>=4.2.0,<5.0.0
huggingface_hub
protobuf

